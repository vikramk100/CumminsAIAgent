<mvc:View
  xmlns:mvc="sap.ui.core.mvc"
  xmlns="sap.m"
  controllerName="cummins.dispatcher.controller.App">
  <VBox fitContainer="true">
    <Bar design="Header">
      <contentLeft>
        <Title text="SAP - Business Network Portal" level="H3" />
      </contentLeft>
      <contentRight>
        <Button text="Notifications" type="Transparent" press="onNavNotifications" />
        <Button text="Help" type="Transparent" press="onNavHelp" />
        <Button text="Settings" type="Transparent" press="onNavSettings" />
        <Button text="User Profile" type="Transparent" press="onNavUserProfile" />
        <Button text="Support Center" type="Transparent" press="onNavSupportCenter" />
      </contentRight>
    </Bar>
    <App id="app" />
  </VBox>
</mvc:View>
