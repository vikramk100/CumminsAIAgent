# MongoDB connection. Do not commit .env with real credentials.
# Copy to .env and set MONGODB_PASSWORD to your actual password.

MONGODB_URI=mongodb+srv://vikramrkavalipati_db_users:password@workorderandconfirmatio.gfkc6h6.mongodb.net/?appName=WorkOrderAndConfirmations
MONGODB_PASSWORD=
MONGODB_DB=sap_bnac

# Gemini (Agentic dispatch). Keep real keys only in .env (gitignored).
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Set to 1 to clear collections before insert (idempotent run)
# CLEAR_COLLECTIONS=0
