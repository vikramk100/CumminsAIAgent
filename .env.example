# MongoDB connection. Do not commit .env with real credentials.
# Copy to .env and set MONGODB_PASSWORD to your actual password.

MONGODB_URI=mongodb+srv://sankethrp_db_user:<db_password>@workorderandconfirmatio.gfkc6h6.mongodb.net/?appName=WorkOrderAndConfirmations
MONGODB_PASSWORD=your_password_here
MONGODB_DB=sap_bnac

# Gemini (Agentic dispatch). Free tier available. Keep real keys only in .env (gitignored).
GEMINI_API_KEY=your_gemini_key_here
GEMINI_MODEL=gemini-2.0-flash

# Set to 1 to clear collections before insert (idempotent run)
# CLEAR_COLLECTIONS=0
